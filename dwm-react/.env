# NOTA: normalmente este archivo .env NO se sube al repo (esta en .gitignore).
# Se subio UNICAMENTE para fines de EVALUACION, para que la profesora vea la
# configuracion. Solo contiene la URL del API (no hay secretos).
VITE_API_URL=http://localhost:5157
